# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets. This repo is public, so a committed key is a published key —
# GEMINI_API_KEY lives in the Vercel project settings, never in a file here.
# .env.example stays tracked: it documents which variables are needed, with no
# values in it.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
